///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, ARA::COM Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : updaterequest_skeleton.h
/// SERVICE INTERFACE NAME            : UpdateRequest
/// GENERATED DATE                    : 2025-02-11 11:52:34
///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// CAUTION!! AUTOMATICALLY GENERATED FILE - DO NOT EDIT                                                   
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#ifndef PARA_COM_GEN_SERVICE_INTERFACE_UPDATEREQUEST_SKELETON_H
#define PARA_COM_GEN_SERVICE_INTERFACE_UPDATEREQUEST_SKELETON_H
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// INCLUSION HEADER FILES
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// @uptrace{SWS_CM_01004}
#include "updaterequest_common.h"
#include "para/com/skeleton/skeleton_interface.h"
/// @uptrace{SWS_CM_01005}
namespace ara
{
namespace sm
{
/// @uptrace{SWS_CM_01006}
namespace skeleton
{
class UpdateRequestSkeleton;
/// @uptrace{SWS_CM_01009}
namespace events
{
} /// namespace events
/// @uptrace{SWS_CM_01031}
namespace fields
{
} /// namespace fields
/// @uptrace{SWS_CM_00002}
class UpdateRequestSkeleton
{
public:
    /// @uptrace{SWS_CM_00191}
    struct PrepareRollbackOutput
    {
    };
    struct PrepareUpdateOutput
    {
    };
    struct ResetMachineOutput
    {
    };
    struct StartUpdateSessionOutput
    {
    };
    struct VerifyUpdateOutput
    {
    };
    /// @brief Constructor
    /// @uptrace{SWS_CM_00002, SWS_CM_00152}
    UpdateRequestSkeleton(ara::core::InstanceSpecifier instanceSpec, ara::com::MethodCallProcessingMode mode = ara::com::MethodCallProcessingMode::kEvent)
        : mInterface(std::make_unique<para::com::SkeletonInterface>(instanceSpec, mode))
    {
        mInterface->SetMethodCallHandler(kPrepareRollbackCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandlePrepareRollback(data, token);
        });
        mInterface->SetMethodCallHandler(kPrepareUpdateCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandlePrepareUpdate(data, token);
        });
        mInterface->SetMethodCallHandler(kResetMachineCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandleResetMachine(data, token);
        });
        mInterface->SetMethodCallHandler(kStartUpdateSessionCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandleStartUpdateSession(data, token);
        });
        mInterface->SetMethodCallHandler(kStopUpdateSessionCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken /*token*/) {
            HandleStopUpdateSession(data);
        });
        mInterface->SetMethodCallHandler(kVerifyUpdateCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandleVerifyUpdate(data, token);
        });
        mInterface->SetE2EErrorHandler([this](const ara::com::e2e::E2EErrorDomain& errorCode, ara::com::e2e::DataID dataID, ara::com::e2e::MessageCounter messageCounter) {
            E2EErrorHandler(errorCode, dataID, messageCounter);
        });
    }
    /// @brief Destructor
    virtual ~UpdateRequestSkeleton() = default;
    /// @brief Delete copy constructor
    /// @uptrace{SWS_CM_00134}
    UpdateRequestSkeleton(const UpdateRequestSkeleton& other) = delete;
    /// @brief Delete copy assignment
    /// @uptrace{SWS_CM_00134}
    UpdateRequestSkeleton& operator=(const UpdateRequestSkeleton& other) = delete;
    /// @brief Move constructor
    /// @uptrace{SWS_CM_00135}
    UpdateRequestSkeleton(UpdateRequestSkeleton&& other) noexcept
        : mInterface(std::move(other.mInterface))
    {
        mInterface->SetMethodCallHandler(kPrepareRollbackCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandlePrepareRollback(data, token);
        });
        mInterface->SetMethodCallHandler(kPrepareUpdateCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandlePrepareUpdate(data, token);
        });
        mInterface->SetMethodCallHandler(kResetMachineCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandleResetMachine(data, token);
        });
        mInterface->SetMethodCallHandler(kStartUpdateSessionCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandleStartUpdateSession(data, token);
        });
        mInterface->SetMethodCallHandler(kStopUpdateSessionCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken /*token*/) {
            HandleStopUpdateSession(data);
        });
        mInterface->SetMethodCallHandler(kVerifyUpdateCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandleVerifyUpdate(data, token);
        });
        mInterface->SetE2EErrorHandler([this](const ara::com::e2e::E2EErrorDomain& errorCode, ara::com::e2e::DataID dataID, ara::com::e2e::MessageCounter messageCounter) {
            E2EErrorHandler(errorCode, dataID, messageCounter);
        });
        other.mInterface.reset();
    }
    /// @brief Move assignment
    /// @uptrace{SWS_CM_00135}
    UpdateRequestSkeleton& operator=(UpdateRequestSkeleton&& other) noexcept
    {
        mInterface = std::move(other.mInterface);
        mInterface->SetMethodCallHandler(kPrepareRollbackCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandlePrepareRollback(data, token);
        });
        mInterface->SetMethodCallHandler(kPrepareUpdateCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandlePrepareUpdate(data, token);
        });
        mInterface->SetMethodCallHandler(kResetMachineCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandleResetMachine(data, token);
        });
        mInterface->SetMethodCallHandler(kStartUpdateSessionCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandleStartUpdateSession(data, token);
        });
        mInterface->SetMethodCallHandler(kStopUpdateSessionCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken /*token*/) {
            HandleStopUpdateSession(data);
        });
        mInterface->SetMethodCallHandler(kVerifyUpdateCallSign, [this](const std::vector<std::uint8_t>& data, const para::com::MethodToken token) {
            HandleVerifyUpdate(data, token);
        });
        mInterface->SetE2EErrorHandler([this](const ara::com::e2e::E2EErrorDomain& errorCode, ara::com::e2e::DataID dataID, ara::com::e2e::MessageCounter messageCounter) {
            E2EErrorHandler(errorCode, dataID, messageCounter);
        });
        other.mInterface.reset();
        return *this;
    }
    /// @brief Send "OfferService" message to Communication Management
    /// @uptrace{SWS_CM_00101}
    ara::core::Result<void> OfferService()
    {
        return mInterface->OfferService();
    }
    /// @brief Send "StopOfferService" message to Communication Management
    /// @uptrace{SWS_CM_00111}
    void StopOfferService()
    {
        mInterface->StopOfferService();
    }
    /// @brief Allows the implementation providing the service method to trigger the execution of the next service consumer method call at a specific point of time
    ///        if the processing mode is set to kPoll.
    /// @note This function requires that ara::com::MethodCallProcessingMode is configured as kPoll by constructor
    /// @uptrace{SWS_CM_00199}
    ara::core::Future<bool> ProcessNextMethodCall()
    {
        ara::core::Promise<bool> promise{};
        auto ret = mInterface->ProcessNextMethodCall();
        if (ret.HasValue())
        {
            promise.set_value(ret.Value());
        }
        else
        {
            promise.SetError(ret.Error());
        }
        return promise.get_future();
    }
    /// @brief In case the call to E2E_check indicated a failed E2E check of the request message, the server application can get notified via an E2E error handler.
    /// @uptrace{SWS_CM_10470}
    virtual void E2EErrorHandler(const ara::com::e2e::E2EErrorDomain& errorCode, ara::com::e2e::DataID dataID, ara::com::e2e::MessageCounter messageCounter)
    {
    }
    
private:
    std::unique_ptr<para::com::SkeletonInterface> mInterface;
    
public:
    /// @brief Method, PrepareRollback
    /// @uptrace{SWS_CM_00191}
    virtual ara::core::Future<PrepareRollbackOutput> PrepareRollback(const ara::sm::FunctionGroupList& FunctionGroupList) = 0;
    /// @brief Method, PrepareUpdate
    /// @uptrace{SWS_CM_00191}
    virtual ara::core::Future<PrepareUpdateOutput> PrepareUpdate(const ara::sm::FunctionGroupList& FunctionGroupList) = 0;
    /// @brief Method, ResetMachine
    /// @uptrace{SWS_CM_00191}
    virtual ara::core::Future<ResetMachineOutput> ResetMachine() = 0;
    /// @brief Method, StartUpdateSession
    /// @uptrace{SWS_CM_00191}
    virtual ara::core::Future<StartUpdateSessionOutput> StartUpdateSession() = 0;
    /// @brief Method, StopUpdateSession
    /// @uptrace{SWS_CM_90434}
    virtual void StopUpdateSession() = 0;
    /// @brief Method, VerifyUpdate
    /// @uptrace{SWS_CM_00191}
    virtual ara::core::Future<VerifyUpdateOutput> VerifyUpdate(const ara::sm::FunctionGroupList& FunctionGroupList) = 0;
    
private:
    void HandlePrepareRollback(const std::vector<std::uint8_t>& data, const para::com::MethodToken token)
    {
        std::uint8_t retResult{1};
        std::vector<std::uint8_t> retData{};
        para::serializer::Deserializer deserializer{data};
        ara::sm::FunctionGroupList _FunctionGroupList_;
        deserializer.read(_FunctionGroupList_);
        auto future = PrepareRollback(_FunctionGroupList_);
        auto result = future.GetResult();
        if (result.HasValue())
        {
            retResult = 0;
        }
        else
        {
            ara::core::ErrorDomain::IdType domainId = result.Error().Domain().Id();
            ara::core::ErrorDomain::CodeType errorCode = result.Error().Value();
            para::serializer::Serializer serializer{};
            serializer.write(0, true, 0, domainId);
            serializer.write(0, true, 0, errorCode);
            retData = serializer.ensure();
            retResult = 1;
        }
        mInterface->ReturnMethod(kPrepareRollbackCallSign, retResult, retData, token);
    }
    void HandlePrepareUpdate(const std::vector<std::uint8_t>& data, const para::com::MethodToken token)
    {
        std::uint8_t retResult{1};
        std::vector<std::uint8_t> retData{};
        para::serializer::Deserializer deserializer{data};
        ara::sm::FunctionGroupList _FunctionGroupList_;
        deserializer.read(_FunctionGroupList_);
        auto future = PrepareUpdate(_FunctionGroupList_);
        auto result = future.GetResult();
        if (result.HasValue())
        {
            retResult = 0;
        }
        else
        {
            ara::core::ErrorDomain::IdType domainId = result.Error().Domain().Id();
            ara::core::ErrorDomain::CodeType errorCode = result.Error().Value();
            para::serializer::Serializer serializer{};
            serializer.write(0, true, 0, domainId);
            serializer.write(0, true, 0, errorCode);
            retData = serializer.ensure();
            retResult = 1;
        }
        mInterface->ReturnMethod(kPrepareUpdateCallSign, retResult, retData, token);
    }
    void HandleResetMachine(const std::vector<std::uint8_t>& data, const para::com::MethodToken token)
    {
        std::uint8_t retResult{1};
        std::vector<std::uint8_t> retData{};
        auto future = ResetMachine();
        auto result = future.GetResult();
        if (result.HasValue())
        {
            retResult = 0;
        }
        else
        {
            ara::core::ErrorDomain::IdType domainId = result.Error().Domain().Id();
            ara::core::ErrorDomain::CodeType errorCode = result.Error().Value();
            para::serializer::Serializer serializer{};
            serializer.write(0, true, 0, domainId);
            serializer.write(0, true, 0, errorCode);
            retData = serializer.ensure();
            retResult = 1;
        }
        mInterface->ReturnMethod(kResetMachineCallSign, retResult, retData, token);
    }
    void HandleStartUpdateSession(const std::vector<std::uint8_t>& data, const para::com::MethodToken token)
    {
        std::uint8_t retResult{1};
        std::vector<std::uint8_t> retData{};
        auto future = StartUpdateSession();
        auto result = future.GetResult();
        if (result.HasValue())
        {
            retResult = 0;
        }
        else
        {
            ara::core::ErrorDomain::IdType domainId = result.Error().Domain().Id();
            ara::core::ErrorDomain::CodeType errorCode = result.Error().Value();
            para::serializer::Serializer serializer{};
            serializer.write(0, true, 0, domainId);
            serializer.write(0, true, 0, errorCode);
            retData = serializer.ensure();
            retResult = 1;
        }
        mInterface->ReturnMethod(kStartUpdateSessionCallSign, retResult, retData, token);
    }
    void HandleStopUpdateSession(const std::vector<std::uint8_t>& data)
    {
        std::uint8_t retResult{1};
        std::vector<std::uint8_t> retData{};
        StopUpdateSession();
    }
    void HandleVerifyUpdate(const std::vector<std::uint8_t>& data, const para::com::MethodToken token)
    {
        std::uint8_t retResult{1};
        std::vector<std::uint8_t> retData{};
        para::serializer::Deserializer deserializer{data};
        ara::sm::FunctionGroupList _FunctionGroupList_;
        deserializer.read(_FunctionGroupList_);
        auto future = VerifyUpdate(_FunctionGroupList_);
        auto result = future.GetResult();
        if (result.HasValue())
        {
            retResult = 0;
        }
        else
        {
            ara::core::ErrorDomain::IdType domainId = result.Error().Domain().Id();
            ara::core::ErrorDomain::CodeType errorCode = result.Error().Value();
            para::serializer::Serializer serializer{};
            serializer.write(0, true, 0, domainId);
            serializer.write(0, true, 0, errorCode);
            retData = serializer.ensure();
            retResult = 1;
        }
        mInterface->ReturnMethod(kVerifyUpdateCallSign, retResult, retData, token);
    }
    const std::string kPrepareRollbackCallSign{"PrepareRollback"};
    const std::string kPrepareUpdateCallSign{"PrepareUpdate"};
    const std::string kResetMachineCallSign{"ResetMachine"};
    const std::string kStartUpdateSessionCallSign{"StartUpdateSession"};
    const std::string kStopUpdateSessionCallSign{"StopUpdateSession"};
    const std::string kVerifyUpdateCallSign{"VerifyUpdate"};
};
} /// namespace skeleton
} /// namespace sm
} /// namespace ara
#endif /// PARA_COM_GEN_SERVICE_INTERFACE_UPDATEREQUEST_SKELETON_H